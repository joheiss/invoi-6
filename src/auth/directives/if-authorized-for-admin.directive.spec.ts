import { IfAuthorizedForAdminDirective } from './if-authorized-for-admin.directive';

describe('IfAuthorizedForAdminDirective', () => {
  it('should create an instance', () => {
    const directive = new IfAuthorizedForAdminDirective();
    expect(directive).toBeTruthy();
  });
});
