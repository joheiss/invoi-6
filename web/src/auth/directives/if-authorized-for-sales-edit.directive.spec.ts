import { IfAuthorizedForSalesEditDirective } from './if-authorized-for-sales-edit.directive';

describe('IfAuthorizedForSalesEditDirective', () => {
  it('should create an instance', () => {
    const directive = new IfAuthorizedForSalesEditDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
